import { EmailVerificationForm } from "@/components/auth/verify-email";

export default async function page() {
    return (
        <div>
            <EmailVerificationForm />
        </div>
    );
}
